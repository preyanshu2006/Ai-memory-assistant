# Only needed for local dev if you want to point at a non-default backend.
# In production, set VITE_API_BASE as an environment variable in your
# hosting dashboard (Vercel/Netlify) instead of committing a real .env file.
VITE_API_BASE=http://localhost:5000/api
